<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- ImageView at the top to display a header image -->
    <ImageView
        android:id="@+id/headerImage"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/hated_math"
        android:scaleType="centerCrop"
        android:contentDescription="Header Image"
        android:layout_gravity="top" />

    <!-- Main container for the content below the image -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="8dp"
        android:layout_marginTop="220dp"> <!-- Margin to prevent overlapping with the ImageView -->

        <!-- Horizontal container for Difficulty and Operation choices -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp"
            android:gravity="center">

            <!-- Difficulty RadioGroup -->
            <RadioGroup
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Difficulty TextView -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Difficulty"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:paddingBottom="8dp" />

                <!-- RadioButtons for selecting difficulty -->
                <RadioButton
                    android:id="@+id/radio_easy"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:checked="true"
                    android:textSize="20sp"
                    android:text="Easy" />

                <RadioButton
                    android:id="@+id/radio_medium"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:text="Medium" />

                <RadioButton
                    android:id="@+id/radio_hard"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:text="Hard" />
            </RadioGroup>

            <!-- Operation RadioGroup -->
            <RadioGroup
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingLeft="16dp">

                <!-- Operation TextView -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Operation"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:paddingBottom="8dp" />

                <!-- RadioButtons for selecting operation -->
                <RadioButton
                    android:id="@+id/radio_addition"
                    android:checked="true"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:text="Addition" />

                <RadioButton
                    android:id="@+id/radio_subtraction"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:text="Subtraction" />

                <RadioButton
                    android:id="@+id/radio_multiplication"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:text="Multiplication" />

                <RadioButton
                    android:id="@+id/radio_division"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="20sp"
                    android:text="Division" />
            </RadioGroup>
        </LinearLayout>

        <!-- TextView for Number of Questions label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Number of Questions"
            android:textSize="20sp"
            android:textStyle="bold"
            android:gravity="center"
            android:paddingTop="16dp" />

        <!-- Controls for adjusting the number of questions -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingTop="8dp">

            <!-- Minus button to decrease number of questions -->
            <Button
                android:id="@+id/button_minus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="-"
                android:padding="8dp" />

            <!-- EditText to display and modify the number of questions -->
            <EditText
                android:id="@+id/edittext_number_of_questions"
                android:layout_width="50dp"
                android:layout_height="wrap_content"
                android:inputType="number"
                android:gravity="center"
                android:textSize="30sp"
                android:text="10"
                android:ems="3"
                android:backgroundTint="#ECEFF1" />

            <!-- Plus button to increase number of questions -->
            <Button
                android:id="@+id/button_plus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+"
                android:padding="8dp" />
        </LinearLayout>

        <!-- Start button to begin the quiz -->
        <Button
            android:id="@+id/btn_start"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:background="@drawable/rounded_square_button"
            android:text="Start"
            android:textSize="40sp"
            android:textColor="#FFFFFF"
            android:padding="16dp"
            android:textStyle="bold" />
    </LinearLayout>

</FrameLayout>
